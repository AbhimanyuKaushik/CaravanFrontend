/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['search.brave.com','localhost','images.indianexpress.com','images.unsplash.com','images.pexels.com','www.shutterstock.com']
    }
};

export default nextConfig;
